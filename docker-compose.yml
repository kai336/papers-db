version: "3.9"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - ./uploads:/app/uploads
      - ./prisma:/app/prisma
    environment:
      - NODE_ENV=development
    command: sh -c "npm install && npx prisma migrate deploy && npm run dev"
